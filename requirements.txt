# Dependencias para el Servidor MCP de Clima
# Versiones compatibles con Python 3.10+

# Core MCP Server
mcp[cli]>=1.0.0

# HTTP Client para API calls
httpx>=0.27.0

# Validación de datos y configuración
pydantic>=2.0.0

# Gestión de variables de entorno
python-dotenv>=1.0.0

# Logging mejorado (incluido en Python standard library)
colorlog>=6.8.0

# Testing (opcional)
pytest>=8.0.0
pytest-asyncio>=0.23.0
